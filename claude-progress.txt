# Claude Progress — Session Handoff Notes
#
# PURPOSE: Human-readable context that persists across sessions.
# Updated at the end of each session (manually or by hook).
# Git-tracked so it survives branch switches and session resets.
#
# FORMAT: Plain text. Keep it scannable. New sessions read this first.

## Current State
Status: clean (all committed)
Branch: elemont
Last Session: 2026-04-07 (Cycle 114)

## What Was Done
- Cycle 114: Backlog drain (GP-02 MEDIUM)
  - GP-02 (MEDIUM): opera/marshal.go + gossip/gasprice/gasprice.go
    (1) validateRulesBounds accepted MinGasPrice==0 → governance could zero-out spam
    floor, enabling free-cost tx flooding. Fixed: Sign()<0 → Sign()<=0.
    (2) suggestTip read MinGasPrice from rules without nil guard → math.BigMax(nil,x)
    panic on corrupted DB. Fixed: nil-to-zero guards for minPrice+pendingMinPrice.
    Tests: TestValidateRulesBounds_MinGasPriceZero + TestSuggestTip_NilMinGasPrice.
  VinuChain commit: 5a8586f
- Cycle 113: Gas Oracle (Area 20)
  - GP-01 (CRITICAL): gossip/gasprice/constructive.go:35 + opera/marshal.go —
    maxTotalGasPower() = AllocPerSec × MaxAllocPeriod / 1e9. validateRulesBounds
    checked MaxAllocPeriod != 0 but not >= 1 second. Setting MaxAllocPeriod=1ns passes
    bounds, floors product to 0, causes big.Int.Div(x,0) panic in constructiveGasPrice
    on every node accepting the governance proposal. Single governance proposal → network
    halt. Fixed: < inter.Timestamp(time.Second) lower bound in marshal.go for both Short
    and Long; defensive max.Sign()==0 guard in constructive.go.
    Tests: TestValidateRulesBounds_MaxAllocPeriodMinimum + TestConstructiveGasPrice_ZeroMaxTotalGasPower.
    Seed born: SEED-LOWER-BOUND (check !=(0) guards on ns durations fed to /1e9 division).
  - GP-02 (MEDIUM, deferred): gasprice.go:134 suggestTip nil MinGasPrice → panic.
  VinuChain commit: be9859b
- Cycle 112: Backlog drain (NC-02 INFO + LOG-09 LOW — batch)
  - NC-02 (INFO): vecmt/no_cheaters.go — vi.validatorIdxs[e.Creator()] for unknown
    ValidatorID returned Go zero value (0), silently treating unknown creator as
    validator index 0 in the fork-cheater filter. If index 0 is clean, unknown events
    pass. Added explicit ok-check guard in both elemont + pre-elemont branches.
    Added buildForkIndexNodeBForks helper + 2 tests (Elemont, PreElemont) — RED before
    fix, GREEN after. Coverage improved.
  - LOG-09 (LOW): ethapi/tx_trace.go — 6 log.Info → lower severity:
    EVM timeout cancel: Info→Warn; per-tx replay log: Info→Debug;
    trace_block/trace_transaction/trace_get/trace_filter defers: Info→Debug.
  VinuChain commit: b82ea9e
- Cycle 111: Logging Hygiene (Area 26)
  - LOG-07 (MEDIUM): payback/payback_cache.go:321 — GetAvailablePaybackByAddress
    used log.Error when called outside block processing. This is a hard invariant;
    wrong fee refunds applied silently. Changed to log.Crit.
    Added TestGetAvailablePaybackByAddress_InBlockProcessing (covers normal path
    through inBlockProcessing guard).
  - LOG-08 (MEDIUM): 7 log.Error → log.Crit escalations in genesis flush closures:
    gossip/evmstore/apply_genesis.go (txs, tx positions, receipts),
    gossip/apply_genesis.go (blocks, block hashes),
    topicsdb/topicsdb.go (topics, logrecs).
    Partial genesis from flush failure is unrecoverable; node must not continue.
  - LOG-09 (LOW, deferred): ethapi/tx_trace.go per-tx log.Info in replay hot paths
    creates log flood + tx tracking oracle. Noted in audit-log.
  - LOG-10 (INFO, deferred): ethapi/api.go SubmitTransaction value field in Info log.
  VinuChain commit: 11b68f7
- Cycle 110: Consensus Vector Clocks (Area 3 / vecmt/)
  - NC-01 (MEDIUM): NoCheaters had 0% test coverage. Added 4 tests covering
    nil selfParent fast path, no-fork all-pass, fork-detected elemont mode
    (merged validator-indexed view), fork-detected pre-elemont mode (raw branch view).
    Fork scenario: two seq=1 events from same creator → engine allocates new branch
    → observer event sees both → creator marked fork-detected → NoCheaters filters.
    Coverage: 0% → 82.6%. Package: 86.0%.
  - NC-02 (INFO): validatorIdxs map miss returns 0 silently → added to backlog.
  - M-01 (FALSE POSITIVE): GetHighestBefore not propagating elemont — intentional design.
  VinuChain commit: 61af720
- Cycle 109: Pruning Subcommands (Area 38 / MEDIUM design gap)
  - Unit 1: gossip/store_prune_gossip.go — PruneEpochData on gossip.Store
    Deletes DAG events, LLR block/epoch votes, vote indexes, BlockEpochStateHistory
    for epochs up to and including pruneUpTo. Batch writes, LRU cache eviction,
    range compaction at ≥100k deletions.
    Tests: TestPruneEpochData_DeletesEvents, TestPruneEpochData_KeepsNewerEpoch.
  - Unit 2: gossip/evmstore/store_prune_receipts.go — PruneReceiptsUpTo on evmstore.Store
    Deletes receipts (ordered scan), tx positions (full scan), internal txs for
    blocks ≤ n. Error propagation from compaction fixed. receiptPruneCompactionThreshold const.
    Tests: TestPruneReceiptsUpTo_* (3 tests).
  - CLI: opera snapshot prune-gossip (--prune.keepepochs=2)
         opera snapshot prune-receipts (--prune.keepblocks=2628000)
    Both registered in cmd/opera/launcher/snapshotcmd.go.
  VinuChain commits: 78c2559, f8b2400
- Cycle 108: Backlog drain — TRC-06 (INFO, ethapi/tx_trace.go)
  - Removed dead `args.Count == 0 ||` operand from Filter() inner loop.
  VinuChain commit: 41132c3
- Cycle 107: Tx Tracing Subsystem (Focus Area 37)
  - TRC-05 (LOW): ethapi/tx_trace.go — traceBlock replay path accessed
    receipts[i] without checking i < len(receipts). DB inconsistency
    (fewer receipts than txs) caused panic. Added bounds check after
    GetReceiptsByNumber; returns descriptive error on mismatch.
    Added TestTraceBlock_ReceiptCountMismatch (RED: panics, GREEN: passes).
  - TRC-06 (INFO): args.Count==0 dead code in Filter() else branch — noted
    in backlog for future cleanup.
  - EVM determinism verified: all capture functions have recover(); tracer
    writes only to tracer-local state; no shared EVM state modified.
  VinuChain commit: 81a6b8f
- Cycle 106: go-vinu Fork Delta (Focus Area 32)
  - GV-01 (INFO): evmcore/state_transition.go — refundGas() had a dead
    guard: `if feeRefund.Cmp(fee) > 0` after min(fee, availableQuota),
    which is provably unreachable. Removed 3 dead lines. Added
    TestFeeRefundBoundaries (5 sub-tests covering nil/zero/partial/exact/full
    quota cases) to evmcore/state_transition_test.go.
  - Fork areas audited: FeeRefundActive flag lifecycle (set at startup,
    no mid-run races), receipt consensus vs storage encoding split
    (intentional and correct), RLP decode fallback chain (field-count
    unambiguous), JS tracer depth balance, subscription TOCTOU (acceptable),
    btcec v2 migration (correct), graphql/RPC FeeRefund exposure (safe).
  VinuChain commit: 5d50c3e
- Cycle 105: lachesis-base DAG Layer (Focus Area 36)
  - LB-01 (INFO): lachesis-base-dev/gossip/dagordering/event_buffer.go —
    removed unused `deps` field from EventsBuffer struct (dead code).
    Recent commits audited: IsImportant wiring, spillIncompletes slow path,
    datasemaphore overflow/shutdown, cheaters list validation — all correct.
  lachesis-base commit: 0896bca4
- Cycle 104: Concurrent Utility Structures (Focus Area 18)
  - CC-01 (LOW): utils/concurrent/ — all 4 thread-safe wrappers had zero tests.
    Added concurrent_test.go: 8 tests (correctness + goroutine-safety under -race)
    for ValidatorBlocksSet, ValidatorEventsSet, ValidatorEpochsSet, EventsSet.
    No production code changed; regression coverage added.
  VinuChain commit: e029db9
- Cycle 103: Consensus Vector Clocks (Focus Area 3)
  - VECMT-01 (INFO): vecmt/median_time_test.go — elemont-gated MedianTime paths
    lacked direct tests. Added TestMedianTimeAllCheaters (all-validators-cheating
    guard) and TestMedianTimeElemont_StableSort (stable sort with equal timestamps).
    No production code changed; regression coverage added.
  VinuChain commit: c96d433
- Cycle 102: P2P Message Handling (Focus Area 7)
  - P2P-01 (LOW): gossip/handler_sync.go — peerRateLimit.Allow() was called after
    msgSemaphore.Acquire(). Semaphore timeout returned nil before rate check, so
    counter was never incremented during overload. Moved rate limit check before
    semaphore so flooding peers are always counted and disconnected. Added 2 tests.
  VinuChain commit: 2aa8093
- Cycle 101: Governance Attack Surface (Focus Area 5)
  - GOV-01 (MEDIUM): opera/marshal.go — validateRulesBounds() did not check the
    compound formula EventGas+(MaxParents-MaxFreeParents)*ParentGas+MaxExtraData*ExtraDataGas
    vs MaxEventGas. Governance could set ExtraDataGas or ParentGas to force
    MaxGasLimit() to return 0, blocking all EVM transactions. Added overflow-safe
    compound check. Added 2 test cases to TestUpdateRulesGovernanceBounds.
  VinuChain commit: 495bf16
- Cycle 100: Backlog drain — IterateEpochPacksRLP it.Error() check (INFO)
  - LLR-01 (INFO): gossip/store_llr_epoch.go — added it.Error() Crit after loop
  VinuChain commit: 2164f6c
- Cycle 99: Epoch Boundary Edge Cases (Focus Area 29)
  - EP-01 (LOW): gossip/store_epoch.go — getEpochStore() nil deref when epoch store
    uninitialized. Added `es == nil` guard. Added TestGetEpochStoreNilGuard.
  - EP-02 (LOW): gossip/store_decided_state.go — ForEachHistoryBlockEpochState()
    silently discarded it.Error() after loop. Added log.Crit on iterator failure.
  VinuChain commit: 07addca
- Cycle 98: Backlog drain — Close() CloseTables error logging (INFO)
  - CL-01 (INFO): gossip/evmstore/store.go + gossip/store.go — changed
    `_ = table.CloseTables(...)` to log at Warn level on error so DB close
    failures are visible in node logs during shutdown.
  VinuChain commit: 6dbb64d
- Cycle 97: Database Migrations Audit (Focus Area 13)
  - MIG-01 (MEDIUM): gossip/store_migration.go — isEmptyDB did not check it.Error()
    after it.Next() returned false. A LevelDB read failure on the Version table
    was silently treated as an empty table, causing migrateData to skip all
    migrations and mark the DB as fully migrated. Fixed: isEmptyDB now returns
    (bool, error); migrateData propagates the error as a fatal startup condition.
  - Added gossip/store_migration_test.go — TestIsEmptyDBPropagatesIteratorError
    and TestIsEmptyDBTrueForEmpty.
  VinuChain commit: 04a007e
- Cycle 96: State Storage Audit (Focus Area 12)
  - ES-02 (MEDIUM): topicsdb/search_parallel.go — scanPatternVariant discarded LevelDB
    iterator errors; searchParallel only returned ctx.Err(). eth_getLogs silently
    returned empty results on DB read failure. Fixed: SetError/Err helpers on
    synchronizator propagate the first iterator error to searchParallel return.
  - ES-01 (LOW): gossip/evmstore/utils.go — ImportEvm dropped iterator error after
    loop (partial batch written, nil returned). Fixed: check it.Error() before
    batch.Write(), matching store_genesis.go/store_migration.go pattern.
  - Added topicsdb/error_propagation_test.go — TestSearchParallelIteratorError injects
    error mid-scan via mock store, asserts FindInBlocks returns the error.
  VinuChain commit: 56ab2b9
- Cycle 95: CLI Security Re-Audit (Focus Area 16) — new tracing CLI hardened
  - TRC-01 (MEDIUM): importTxTraces rlp.NewStream(reader, 0) → unlimited decode
    for generic readers. OOM risk from crafted import files. Fixed: use
    maxImportStreamSize (600 MB).
  - TRC-02 (LOW): exportTraceTo rlp.Encode error silently discarded → corrupt
    trace file with no error returned. Fixed: propagate the error.
  - TRC-03 (LOW): exportTraceTo/deleteTraces called GetBlockTxs(i, GetBlock(i))
    without nil guard → nil dereference panic on sparse block ranges. Fixed:
    skip nil blocks.
  - TRC-04 (LOW): deleteTxTraces/exportTxTraces ParseUint(64-bit) → idx.Block
    (uint32) silent truncation for values > MaxUint32. Fixed: parseBlockNumber
    helper with bitSize=32.
  - Added cmd/opera/launcher/txtracingcmd_test.go — 5 tests.
  VinuChain commit: 674bbf9
- Cycle 94: LLR Streaming Protocols Audit (Focus Area 10)
  - LLP-01 (LOW): gossip/handler_sync.go — EventsStreamResponse acquired and
    immediately released peerStreamQuota (no-op). BVs/BRs/EPs hold the slot
    until processor done() fires; DAG events use peerEventQuota in handleEvents.
    Removed the dead acquire/release block, added explanatory comment.
  - Added gossip/peer_ratelimit_test.go — 6 tests for peerEventQuota and
    peerStreamQuota (acquire/release lifecycle, RemovePeer, concurrent safety).
  VinuChain commit: e5fc5a5
- Cycle 93: Serialization from Untrusted Peers Re-Audit (Focus Area 17)
  - SER-NEW-01 (LOW): inter/transaction_serializer.go — access list allocation cap tightened
    from ProtocolMaxMsgSize/24 (436,906 entries → 19.2 MB alloc) to
    ProtocolMaxMsgSize/accessListEntrySize (238,290 entries → 10 MB alloc)
    New accessListEntrySize=44 constant documents the bound
  - Added inter/transaction_serializer_test.go — 8 tests covering all tx types,
    cap invariant, and unknown-type error path (previously zero direct tests)
  VinuChain commit: a84c2b7
- Cycle 92: Logging Hygiene (Focus Area 26) — 2 findings fixed
  - LH-01 (LOW): evmcore/state_processor.go — log.Info→log.Error + remove receipt struct
    VinuChain commit: c56e6a2
  - LB-01 (MEDIUM, lachesis-base): kvdb/flushable NotFlushedSizeEst() data race fixed
    lachesis-base-dev commit f90e4255 (branch fix/deferred-audit-c57)
    Requires new lachesis-base tag + go.mod bump to resolve TestSFC/SFC_upgrade race
- Cycle 91: Event Validation Pipeline (Focus Area 9) — EC-01 (HIGH) IsBan asymmetry fixed
- Cycle 90: Tx Tracing Subsystem — 3 findings fixed (TT-01, TT-03, TT-04)
- Previous sessions: Cycles 50-89 — full 32 focus area coverage, all findings fixed

## What's Next
- Cron audit: /loop active at 15m intervals — next cycle full audit (backlog empty)
- lachesis-base: publish new tag from fix/deferred-audit-c57 branch, update VinuChain go.mod
- Areas eligible for re-audit: all except FA20/FA26/FA3 (last 3 cycles)
- Noted in cycle 97: same isEmptyDB pattern in compactdb and legacy_migrate (INFO/CLI-only)
- Seed file: update SEED-LOWER-BOUND in docs/audit/attack-seeds.md

## Blockers / Open Questions
- TestSFC/SFC_upgrade fails with go test -race ./... due to pre-existing lachesis-base race
  (Flushable.NotFlushedSizeEst has no lock). Fix committed to lachesis-base-dev but not yet published.

## Key Decisions Made
- go-ethereum replaced with go-vinu fork: receipt.FeeRefund, payback changes, JS tracer fix, per-connection sub limits
- Podgorica upgrade gates payback system activation; QuotaCacheAddress comes from Rules.Economy
- Commit messages never reference audit item IDs (C-07, H-17 etc.) — describe the change instead
- docs/, CLAUDE.md, .claude/, gitignore/, build/ are all gitignored — never git add these paths
- Per-peer event processing quota added for heavy check queue (30 events/peer)
